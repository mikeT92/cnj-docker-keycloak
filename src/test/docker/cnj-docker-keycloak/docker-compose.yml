# docker-compose.yml
# ----------------------------------------------------------------------------
# Composes Docker containers and volumes to a runnable Java EE application
# ----------------------------------------------------------------------------
#
---
version: '3'

services:

  keycloak:
    image: hmfwpcnj/cnj-docker-keycloak:latest
    ports:
      - "9080:8080"
    environment:
      KEYCLOAK_USER: admin
      KEYCLOAK_PASSWORD: admin123
#      KEYCLOAK_IMPORT: /tmp/cloudtrain-realm-export.json
